# E046 — Native dominated end-turn guard integration

Issue #82. Hypothesis: using the already-audited E015 `filter_end_turn` before native guided/planned Jev decisions will prevent a clearly dominated end turn without changing explicit Astra actions or cases where card play can have an unknown downside. The simple baseline is current `rsi.campaign`: it passes unfiltered native candidates to guided Jev. E045's first Sludge Spinner battle supplied a factual failure: turn3, 1 energy/0 block, three legal Defends, 14 incoming, Jev chose end turn and lost14 HP. The exact ignored raw trace SHA-256 and sanitized input/candidates are in `frozen-dominated-end.json`; source fingerprint `4fc73707aae030e966b18fc28915f45d373ed8d08590a657287e2b04a36a9016`. E015 guard already reports all three Defends as witnesses; this is a prior-state diagnostic, not a replayed battle or 5 HP actually saved.

Fixed controls before implementation: on that frozen native state, the integrated ordinary candidate set must exclude end turn, keep every offered Defend legal, and the planner must return a legal card even with no end-turn candidate. Exact expert choices and raw `jev` policy retain the original candidate set. Synthetic controls add an unknown enemy power, Ice Cream energy retention, no incoming attack, or no useful legal basic card; the guard must leave end turn available in each. Do not broaden the E015 safety proof or invent enemy mechanics.

Live compatibility input is the same unreset E045 run `4BD4R62AQHNK`, floor2 MAP, 68/80 HP, current fingerprint `57a1e818d4cf046cd986ef7a96699a57dbf2b329ef03328bfad1e3f181f58727`. Only next node is Monster. `floor2-plan.json` binds this state and covers through floor3 MAP, with Jev handling combat/rewards. After implementation and tests are committed, run `python3 -m rsi.campaign --expected-run-id 4BD4R62AQHNK --combat-policy floor_guided --floor-plan experiments/E046/floor2-plan.json --pause-on-danger --danger-hp 8 --max-actions 80 --max-seconds 300 --max-usd 0.10 --output artifacts/runs/e046-floor2.json --execute`. This also counts as E045's floor2 scheduled plan. One writer, no reset or replay. Record selected seed/run, HP, cards, rewards, accepted/rejected actions, guard triggers, Jev usage and raw trace hash. Game expected v0.111.0, MCP v0.15.0, Jev `typesafe/jev-1.13`.

Decision gate: merge the native integration only if frozen factual input and all synthetic controls pass and the one live room has no new controller error or rejected action. This is execution/mechanism compatibility, not a matched win-rate improvement. If the guard causes a regression, preserve the failed iteration on a closed PR branch and keep E045 paused for reassessment. Commit implementation before evaluation, results afterward, PR comment before merge/close.
