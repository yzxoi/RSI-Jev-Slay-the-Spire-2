# E040 — One Astra plan per floor, Jev execution

Issue #71. The current controller delegates ordinary combat to an approximate hand planner and macro/potion decisions to Jev; a low-HP threshold can pause before checking a known kill. Test a different division of labor: Astra writes one compact plan at a map boundary, Jev chooses legal actions during the next room, and the native controller rechecks every action against fresh state. The plan is advice, not a forced command or game-state edit.

The first experiment tests protocol feasibility, not win-rate improvement. Baseline is the merged native `retaliate` controller without floor guidance. No simultaneous native baseline can run on the player's one live game, and an offline choice comparison would not establish a counterfactual run. E039 engine branches and E006 automatic Astra routing remain separate experiments.

Fixed pilot input: existing Ironclad A0 run `EFUZ4NHFCXBT`, floor 7 MAP, HP 21/80, gold 90, three potions, only next node row 7 col 2 Unknown. Freeze exact start fingerprint and guidance before execution. One Astra strategic plan, at most 80 accepted actions, 300 controller seconds, $0.10 Jev. Stop at the next MAP on floor 8 after resolving room/rewards, genuine game over, or an explicit compatibility/safety boundary. The run is not restarted or edited. Preserve actual loss/stall if one occurs.

Protocol gate: the plan must bind run ID, start floor, next floor and exact initial state hash; it expires after the target map. Jev receives the plan in every discretionary request and a computed proposal in combat, while the native legality, freshness, single-writer and lethal-end-turn guards remain. Tests check stale plan rejection and boundary. Promote the protocol only if the tests pass, at least one real Jev request contains the plan, and the pilot has no plan-attributable illegal/stale execution or missed boundary. This does not prove stronger play. Measure Jev requests/cost, controller time, floor completion, HP, action count, plan adherence, and all stops.

The floor 7 plan is `floor7-plan.json`; its start fingerprint is `66fc22d7196c20cea400d08723b5652f67491dcac2dd9eb879bb40549f3133e0`, computed from a read-only raw MCP state copied to ignored `artifacts/runs/e040-floor7-start.json`. The plan is conditional guidance, not a scripted action list; it states the HP/resource objective, combat priorities, potion thresholds, event risk and reward criteria. Each implementation/fix iteration receives its own commit before evaluation; test results and rationale follow in later commits and PR comments.

Iteration 1 implementation SHA `9b54853cb35e96c27c7c9c4beedcab1f41b99c67`. `python3 -m unittest discover -s tests -v`: 52/52 passed. Read-only native dry run matched the frozen start plan; trace hash `1ca71a514b05fa0c7bb705bbb17caeb4c2e9b800a4e51ececc8b4dfee35038c0`. Pilot command: `python3 -m rsi.campaign --expected-run-id EFUZ4NHFCXBT --combat-policy floor_guided --floor-plan experiments/E040/floor7-plan.json --max-actions 80 --max-seconds 300 --max-usd 0.10 --output artifacts/runs/e040-native-floor7.json --execute`. Game v0.111.0, native MCP 0.15.0, Jev typesafe/jev-1.13. `pilot1.json` contains compact results and the raw local trace hash.

Pilot 1 completed the floor 7 to floor 8 boundary in 3 accepted actions, 0 rejections, 3.281 controller seconds, 1 Jev request costing $0.00012873, and 1 Astra floor plan with no extra Astra action. Jev received the plan and chose the Light door's random two upgrades over one card removal; the game upgraded Strike and Battle Trance. HP stayed 21/80. No combat occurred. Protocol delivery and boundary passed, but this does not test whether Jev can execute the combat portion or whether the plan improves outcomes.

## Preregistered continuation: first combat coverage

Keep the same unreset game run, starting at floor 8 MAP with 21/80 HP. Continue through at most four planned floors (floor 8→9, 9→10, 10→11, 11→12), each with exactly one freshly state-bound Astra plan committed before execution and the same `floor_guided` controller. The route after each room remains an actual choice; no future hidden reward or encounter is assumed. Per-floor caps are 80 accepted actions, 300 controller seconds and $0.10 Jev. Stop after the first resolved combat and its rewards at the next MAP, a normal terminal, or an explicit safety/compatibility boundary. Do not revise an already executed floor plan; preserve all intervening event/rest/treasure results. Record all floors, including those without Jev requests. Combat coverage requires at least one discretionary Jev combat choice with the plan included, normal actual actions and an observed combat outcome. It is not a strength comparison; no victory-rate promotion follows from this continuation.

Floor 8 plan committed at `3d45eed4ea19ba04303d931eb02d52c70f3f56c2` with frozen fingerprint `3b2029d4b31be82cfaf0e973c0d578199ad8f6fccafe474b517e0b3fecd1d2d1`. Same pilot command as floor 7 with `floor8-plan.json` and `e040-native-floor8.json`. It completed the Rest Site in 3 accepted actions / 0 rejections / 3.559 seconds. Jev received the plan and chose HEAL, restoring 21→45/80 HP; 1 Jev request cost $0.000120246. Trace hash and compact result are in `pilot2-floor8.json`. No combat yet.
