# E079 — frozen Boss-entry potion-plan replay

Issue: [#150](https://github.com/yzxoi/RSI-Jev-Slay-the-Spire-2/issues/150). Baseline: merged `origin/main` `a7c2469f86777f2cef2d1bef6fd68ac30fe42ca7`, deterministic `retaliate` combat planner with Jev for any card-selection macro decision. E078's new-seed full-run cohort did not exercise its persistent-buff Boss opener; this experiment directly tests two observed E077 Boss entries. It is a battle-branch experiment, not a win-rate estimate or a real-game victory.

Hypothesis: an Astra-authored, entry-state-bound opening will improve the Boss battle by spending potions whose effects last through the fight. The [frozen fixture](frozen-boss-prefixes.json) contains only action commands, not game states, credentials or game binaries. Silent A10 `e077_a10_006` replays 249 commands from E077 source wire SHA-256 `745be8d3dac3f9080dddeba9d567b9eb5a356817263bf5551c6f55572f581406` to entry state `53643a6588be8a1427840434e145874b4ed960911b7a35b253c13e2c4cb0f65b`; treatment uses Dexterity Potion then Explosive Ampoule, reserving Flex Potion. Ironclad A10 `e077_a10_007` replays 200 commands from source wire SHA-256 `3e9e5747be0c29bc3d67c9251e4b7a771c594d6f9cd942cab2ef31b57d5517e7` to entry state `1c8723e65f738297e674fbd768326032bef4addcf32be7d267fb33d13870e491`; treatment uses Liquid Bronze then Dexterity Potion. Original source boss battles were defeats.

For each case, replay the same prefix in two fresh CLI processes and verify exact entry-state parity before branching. Baseline continues with `retaliate`; treatment executes the named legal potions before any card, reading fresh state after each, then uses the same planner. Matched Jev caches identical macro requests across arms. Stop on Boss clear, game-over defeat, 300 decisions or 180 seconds. Shared limit: 100 Jev calls/$0.10. Pinned game v0.111.0, sts2-cli `084d1aa3d8e118ca7ce8d8774ad16d6be9c92367`, Jev `typesafe/jev-1.13-20260917`. Command: `python3 -m scripts.evaluate_boss_plan_e079`. Preserve ignored raw traces; publish code/dependency/fixture hashes, parity, potion consumption, boss outcome, turns, HP, enemy HP, Jev usage and verified raw trace hashes. No edited HP/rewards/RNG/victory flags.

Decision: all four replays must match their exact frozen entry states and finish without execution error; both treatment plans must consume their named potions; at least one treatment must clear its Boss while its paired baseline does not, with no baseline clear converted to defeat. Only then consider promoting an opt-in Boss plan for broader full-run and native tests. Otherwise close as negative/inconclusive. Boss-specific branch evidence cannot be reported as a full-run win.
