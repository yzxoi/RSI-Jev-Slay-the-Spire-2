# E055 — retained Beckon self-loss

Issue: [#102](https://github.com/yzxoi/RSI-Jev-Slay-the-Spire-2/issues/102). Hypothesis: the current native lethal-end-turn predicate and Toxic-only reservation omit `BECKON`'s direct `HpLoss` on retained cards. A narrow candidate constraint that reads the card's structured ID and dynamic value can prevent demonstrated lethal energy spending without pretending to simulate future draws or unknown mechanics.

Baseline: `origin/main` at `2d43577`, with `--combat-policy room_guided`; existing `reserve_toxic_energy` and `end_turn_will_kill_player` behavior. Fixed retrospective inputs: **every 57 Soul Fysh floor-17 `before` combat decision states** in E054 run `DCEND0WRAPGL`, across four SHA-pinned native traces listed in `scripts/freeze_beckon.py`. Character Ironclad, ascension 0, native game v0.111.0, mod v0.15.0, Jev `typesafe/jev-1.13-20260917`. The tracked gzip fixture retains only player, hand, enemies, relics, native predicate and offered candidates. Generate it with:

```sh
python3 scripts/freeze_beckon.py --source-root /Users/yzxoi/RSI-Jev-Slay-the-Spire-2-e054/artifacts/runs --output experiments/E055/frozen-soul-fysh.json.gz
```

Decision rule: promote if the fixed terminal-risk state is flagged before the final energy is spent, the legal Beckon survival option remains, nonlethal controls are preserved, and tests cover zero/multiple Beckons, energy shortage, sufficient Block, lethal attack without Beckon, and plausible immediate kill. Count changed candidate sets over all 57 states. A changed offline action set is **not** a counterfactual battle outcome or win. Budget: no new native run in this issue; one fixed-state replay plus synthetic mechanic tests. Any full-run strength claim requires a separately preregistered fresh game.

The raw native traces remain local under ignored `artifacts/runs/`; this fixture is a compact, sanitized decision-state projection. Implementation, exact tested SHA, commands, results and decision will be appended as committed iterations.

## Iterations and evaluation

- `c1dea85`: froze 57 native boss decision states before implementation. Fixture SHA-256: `cbfbfd9f41e6b3ba78613dc92a66c838492e3fa8375502c21e800a83d44d1c61`.
- `5619856a4b83b752589182e377c387227f988347`: first runnable Beckon projection and candidate reservation. `python3 -m unittest tests.test_beckon_reservation tests.test_toxic_reservation tests.test_native_endturn_guard` failed 2/17 tests: the old nonlethal-Strike proof abstained whenever the target had Vulnerable, including the exact E054 final-energy state. The initial replay invocation also used a direct script path, which lacks the project root on `sys.path`; it was not a policy failure. Both failures are retained in Git history.
- `bc85609b11eb20616c38bde8712601a85664d12b`: allow a conservative 2× upper bound for Vulnerable **only in the Beckon guard's known Strike proof**, preserving the Toxic baseline's older abstention. Commands: `python3 -m unittest tests.test_beckon_reservation tests.test_toxic_reservation tests.test_native_endturn_guard`; `python3 -m scripts.replay_beckon --output experiments/E055/replay-v2.json`; `python3 -m unittest discover -s tests`. Outcomes: 17/17 focused tests and 77/77 repository tests passed. The fixed replay changed 2 of 57 candidate sets. On case 55 (turn 12, HP13/Block14/energy1, Attack24), it removed both nonlethal Strikes and retained the playable Beckon, whose projected immediate loss is 10 rather than 16. On case 41, it removed a potentially lethal end turn with two Beckons in hand. The projection identified 6 lethal-looking states, including cases 55 and 56 where native `end_turn_will_kill_player` was false. The final energy-zero state is a detection, not a rescue. Compact evidence: `replay-v2.json`.

These are **retrospective candidate replay and synthetic mechanic tests**, not a simulated Soul Fysh victory. The CLI headless engine does not expose importing this exact native combat state/RNG; comparing a newly generated headless seed would answer a different question. No new native action or Jev call was made in this experiment, so new model cost is $0. The replay has one character, one seed, one boss, and cannot estimate full-run strength or high-ascension win rate. Future drawn Beckons and unknown on-play effects remain outside the proof; every game action still requires fresh-state validation.

Decision: promote this narrow guard because it meets the issue's fixed-state and control criteria, while testing actual battle outcomes only in a separately preregistered fresh native run. Merge reason: the real fatal energy-spend candidate is excluded without deleting the observed Beckon survival option; existing Toxic behavior and all repository tests pass. The result does not claim the alternative wins the boss or the run.
