# E017 — Native preview interpretation

Issue #29. Native MCP `GameStateService.Run.cs` clones dynamic vars and calls UpdateDynamicVarPreview, exporting PreviewValue as current_value. Previous adapter reapplied owner modifiers and ignored CalculatedDamage, assigning zero damage to Perfected Strike. Correct these defects; keep target vulnerability separate with disclosed rounding limits. Include Plating in end-turn block scoring, without incorrectly adding it to current Body Slam block. Headless states lack this native-only field and keep their existing scoring. Slow and other target effects remain unsupported, not claimed fixed.

Validation: replay all accessible native before/selected/after card transitions from attempts1/2, compare old/new immediate damage and block predictions by card. This is observed transition validation, not a counterfactual win claim. Require lower prediction error and focused regression tests before deploying. Native next attempt uses the fixed planner, review of strategic choices and danger escalation; report expert involvement and all losses. Add an explicit macro-review segment boundary so Astra can inspect reward/build decisions without a writer conflict.
