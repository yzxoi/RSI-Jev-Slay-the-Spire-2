# E017 — Native preview interpretation

Issue #29. Native MCP `GameStateService.Run.cs` clones dynamic vars and calls UpdateDynamicVarPreview, exporting PreviewValue as current_value. Previous adapter reapplied owner modifiers and ignored CalculatedDamage, assigning zero damage to Perfected Strike. Correct these defects; keep target vulnerability separate with disclosed rounding limits. Include Plating in end-turn block scoring, without incorrectly adding it to current Body Slam block. Headless states lack this native-only field and keep their existing scoring. Slow and other target effects remain unsupported, not claimed fixed.

Validation: replay all accessible native before/selected/after card transitions from attempts1/2, compare old/new immediate damage and block predictions by card. This is observed transition validation, not a counterfactual win claim. Require lower prediction error and focused regression tests before deploying. Native next attempt uses the fixed planner, review of strategic choices and danger escalation; report expert involvement and all losses. Add an explicit macro-review segment boundary so Astra can inspect reward/build decisions without a writer conflict.

Audit v1 found nonsensical negative damage when killing an enemy: native enemy indices compact after deaths, so equal indices can refer to different monsters. Preserve the first report as an audit defect. Fix comparison to require unchanged enemy count and matching enemy ID/max HP; killing transitions remain excluded unless stable identity can be proven. This is a reporting fix, not a game-policy change.

Corrected audit v2: 72 stable damage transitions, absolute error116→46; 153 block transitions, error14→10. Remaining large errors identify Slippery (limits HP loss to1 until consumed); six small mismatches identify Slow (10% per earlier card, not its constant power amount). Verify these mechanics from the locally owned game binary, keep that source private, then add native-only target metadata and tracked cap consumption/Slow order to the approximate planner. Remaining rounded owner previews are still not exact.

Preview audit v3 (4ac7c67): stable damage predictions match71/72 actual transitions, total absolute error1 (previous57/72, error116). Block matches151/153, total error10 (previous147/153, error14); remaining block mismatches include unmodeled triggers. Eighteen focused tests pass. Apply as a computation fix, with no victory-rate inference from reused traces.

Attempt4 first combat ended at80/80 HP (after Burning Blood), now choosing a real reward. Twin Strike's fixed two hits are not exported as a dynamic variable; add an explicit ID rule plus per-hit block/Slippery consumption before using it. This is a documented limited metadata rule, not general natural-language parsing. Subsequent emitted repeat/hits variables are honored.

Native attempt4 MDGN86P6N6BF continues without rollback. Floors2/3 ended full HP; floor6 reached 63/80. Expert macro choices: Fishing Rod, Twin Strike, Pommel Strike, Bludgeon, Shrug; numeric combat plus Jev potion consultation. Floor6 skip exposed a distinct reward re-open bug; tracked separately in E019. No completed victory yet. Native preview v3 audit: 71/72 damage transitions exact (absolute error1 vs baseline116); 151/153 block transitions exact (error10 vs14). Mechanic audit is not a win-rate measurement.
