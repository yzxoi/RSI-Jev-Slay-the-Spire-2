# E060 — native bundle confirmation compatibility

Issue: [#111](https://github.com/yzxoi/RSI-Jev-Slay-the-Spire-2/issues/111). E059 run `EC0JQCUP6QMC` is paused at floor1 `BUNDLE_SELECTION` after an accepted Neow bundle choice. Fresh native state advertises `confirm_bundle`, has `bundles=null`, and fingerprint `9ae4c5fb9957e62cd16b810fd1b887bccdc7f53fea199afeac0032b983e0fd0b`. `rsi.scenes.candidates` currently iterates absent bundles for confirmation and returns no action. E059 raw trace SHA-256 `f6763d62cfcdc43fc0f279663c193a7ba4b4168871f848b47e558a6733e470a3`; zero uncertain action deliveries.

Hypothesis: confirmation is a zero-argument native action. Generate one `confirm_bundle` candidate when advertised, independent of `bundles` contents; keep indexed `choose_bundle` when that action is advertised before selection. Baseline: `origin/main` commit `cded43435b18bba62191f3ece9f20736c11097d4`. Fixed test states: the observed E059 action/bundle shape; a synthetic two-bundle choice shape; and a synthetic no-bundle-action shape. Game v0.111.0, MCP mod v0.15.0. No model call is needed to validate this adapter.

Decision rule: candidate adapter outputs exactly the advertised legal action with no invented option index; all repository tests pass; one fresh state-bound native `confirm_bundle` is accepted on the same E059 run, advancing beyond bundle selection. Preserve the action's raw trace SHA and exact code SHA. Merge for execution compatibility if these pass, then incorporate the merge into E059 before resuming its controller. No battle/strength claim follows from this fix.
