E033 baseline a0_e033_dev_001 reaches Act2 Amalgamator but repeats initial event choices after selecting two cards. The async event Task is local to DoChooseOption and lost when a card-selection boundary returns. DoSelectCards pumps once but does not wait for the originating event Task; subsequent decisions can re-enter Chosen and remove cards repeatedly. No state-progress guard eventually stops it; this is an adapter error, not defeat.

Hypothesis: retain the in-flight event Task across selection/reward boundaries and wait only until it finishes or advertises the next actual pending selection. Never repeat event actions to force progress. Use bounded wait with explicit error on unresolved task; preserve normal non-event code.

Fixed inputs: exact recorded engine wire prefix from E033 planned a0_e033_dev_001 through the first Amalgamator select_cards, no model calls. Require no duplicated combine action, intended deck delta and completed event/map progression. Also replay E018 Dense Vegetation prefix and existing checks. Then rerun E033 development with the same repair for both policies; retain original error reports. Only local headless source/DLL copy changes, never actual Steam game or victory values.
