# E059 — fresh native Ironclad A0 attempt

Issue: [#110](https://github.com/yzxoi/RSI-Jev-Slay-the-Spire-2/issues/110). Objective: one fresh, unmodified visible native run using merged E055 delayed Beckon loss and E056 predictive HP review. This is an out-of-sample execution/outcome trace, not a paired strength estimate. The currently healthy game is at `MAIN_MENU`; no other writer is active. Single writer is enforced by the cross-worktree lock, and actions are never retried after uncertain delivery until fresh state establishes their effect.

Fixed implementation baseline: `origin/main` merge `cded43435b18bba62191f3ece9f20736c11097d4`. Ironclad A0, game v0.111.0/mod v0.15.0, Jev `typesafe/jev-1.13-20260917`, normally generated seed captured on embark. The controller will use `--combat-policy retaliate --auto-combat-selections --pause-on-danger --danger-hp 20`, Jev for macro decisions, and bounded segments of at most 200 native actions, 900 seconds, and $0.10 reported Jev. Astra actions, if needed, are state-hash-bound individual choices. No HP/reward/victory edits or rollback. Stop at normal GAME_OVER or an unrecoverable external fault. Keep full raw traces under ignored `artifacts/runs/` and publish compact trace hashes, exact tested SHAs, actions, cost, interventions, E055/E056 trigger counts, game outcome and save status.

Decision rule: record the normally generated attempt whether win or loss. An evidence-only PR can merge after all action/trace hashes reconcile; a distinct code defect gets a new preregistered issue/PR. Compatibility, battle results, full-run result and save verification are separate claims. N=1 gives no win-rate or high-ascension estimate.

Menu setup: state-bound `open_character_select` was accepted at tested SHA `eaf5ef3` (raw trace SHA-256 `f5ec8a384d80bdcc2f2832a43785a3edf695a25a185ba51b0ef70de464f4625a`). Ironclad was already selected at A0. State-bound `embark` was accepted at tested SHA `a4c8681` (raw trace SHA-256 `bc547d7c0d467e167327746b5d731bf23af06863b4bf69d935577cfeaba03bca`). New game run ID: `EC0JQCUP6QMC`; floor 1, 80/80 HP, Neow event. These two menu actions are setup, separate from in-run campaign actions.

First controller segment at tested SHA `160610f736d672a08bcc8234a82c258aaf578d46`, command `python3 -m rsi.campaign --expected-run-id EC0JQCUP6QMC --max-actions 200 --max-seconds 900 --max-usd 0.10 --output artifacts/runs/e059-segment01.json --execute --combat-policy retaliate --auto-combat-selections --pause-on-danger --danger-hp 20`, accepted two actions (Neow event choice and `choose_bundle`), made two Jev calls ($0.000249018), then stopped with `No supported action` on `BUNDLE_SELECTION`. Raw trace SHA-256 `f6763d62cfcdc43fc0f279663c193a7ba4b4168871f848b47e558a6733e470a3`, zero explicit rejections or uncertain deliveries. Fresh state advertises zero-argument `confirm_bundle` with `bundles=null`; issue [#111](https://github.com/yzxoi/RSI-Jev-Slay-the-Spire-2/issues/111) / E060 isolates the scene adapter fix. This is a **compatibility fault after two accepted actions**, not a battle outcome. The same native run is intact at floor1 and will resume only after E060 is validated.

## Progress checkpoint after floor 31

E060's `confirm_bundle` fix was merged and incorporated at `2e25ea1`, after which the same run resumed. The live native game is currently at floor 31 `REWARD`, 12/90 HP after Burning Blood, run ID `EC0JQCUP6QMC`, state fingerprint `f016df5bc47df45e457071c342ed50d989b3d5b606552fab258bd1093ad035c6`. This is **not** a full-run win. Game v0.111.0/mod 0.15.0 remain unchanged. One writer operated the game; all accepted state-bound choices, including corrections to mistaken damage estimates, were committed before execution. No rejections or uncertain deliveries are reported in the 13 campaign segments.

[`progress.json`](progress.json) lists all 78 result records and their exact tested code SHAs. Its 78 trace hashes were independently recomputed from the ignored local raw traces and all matched. Across these records: 412 accepted actions, 99 Jev calls, $0.024748836 reported Jev cost. These include 65 state-bound manual native steps, so this trace demonstrates an Astra intervention bottleneck rather than low-Astra-cost autonomy. Segment commands use the registered campaign flags above with a numbered `e059-segmentNN.json` output; manual choices use `python3 -m rsi.native_step --choice experiments/E059/<choice>.json --expected-run-id EC0JQCUP6QMC --output artifacts/runs/e059-<choice>.json --execute`. Every choice JSON and corresponding compact result is in this directory; raw `decisions.jsonl` stays ignored locally.

Floor 30's three reattaching centipede segments required potion-assisted defense at 43→15 HP, then retained attacks cleared all segments on turn 5. Floor 31's Exoskeleton `HARD_TO_KILL_POWER` capped each hit at 9: an advertised 14-damage Unrelenting dealt only 9. The initially planned two-kill line was abandoned **before** the next action; three-hit and multi-hit lines cleared the encounter at 6 HP, then Burning Blood healed to 12. This is observed native behavior, not a simulator counterfactual. A separate preregistered experiment should test whether hit-cap-aware planning improves choices in batch simulation. E061 separately tests whether a one-turn expert review lease can reduce repeated Astra pauses without bypassing safety guards.
