# E045 — Fresh room-guided Ironclad A0 campaign

Issue #80. Feasibility hypothesis: beginning at full HP, a state-bound Astra plan at each map floor plus Jev execution of ordinary combat, rewards, cards and events can produce a genuine three-act A0 victory with far fewer Astra decisions than E013's 213-choice assisted victory. E042's mixed-policy continuation started at floor12/7HP and lost normally to Waterfall Giant floor17; it is a diagnostic baseline, not a matched control. Success requires native `GAME_OVER is_victory=true`; a single result cannot establish win rate, stability, high-ascension or other-character performance.

Fixed input procedure: use the **next normally generated** Ironclad A0 run from the current native `GAME_OVER` state; do not choose or reroll a seed. Current prior run `EFUZ4NHFCXBT` ended at floor17 with `is_victory=false`. The read-only initial GAME_OVER fingerprint is `cb8031e52ab234b44472bf4abff3a3e1f54ddc05fccd1061c7992e86b415bf3c`, with only `continue_game_over` advertised. Advance native summary/menu actions one at a time under the one-writer lock, verifying fresh state and legal indices before each. Record the new game seed/run ID immediately. No game-state or HP/reward/victory edits. Game expected v0.111.0, MCP v0.15.0 and Jev `typesafe/jev-1.13`; exact manifests accompany each segment.

Use E040 `floor_guided` at MAP nodes with one exact-state-bound committed plan to cover the chosen room through next MAP, including reward/card/route choices by Jev. At a forced entry or a room needing a precisely known opener, E041 `room_guided` may be used; count that plan separately. Explicit safety decisions at lethal/unknown boss mechanics are allowed and counted. At each room plan, compare current route, HP, deck, potion slots, expected fight and boss special effects; preserve life and spend consumables when their verified current impact beats holding them for an uncertain later state. Recompute hand and target indices after every action. Stop each controller segment at the next MAP, genuine terminal, or explicit danger/compatibility/budget boundary. No reset after a bad outcome.

Per segment maximum: 80 accepted actions, 300 controller seconds, $0.10 Jev. Global attempt maximum: $1 Jev and 100 total Astra plans/reviews; target <=60 scheduled per-floor plans plus <=40 extra reviews. Record actual cost and counts, not just targets. Preserve ignored raw traces under `artifacts/runs/`, publish compact results/trace SHA-256 and exact tested code commit. Commit each menu/plan implementation before evaluation and each result afterward. Report every battle, card reward, potion use, stall, defeat and terminal state separately. If the run ends or the global cap is reached, comment the outcome on the experiment PR and merge only reusable, supported execution changes; retain negative strategy history on a closed PR branch.

For menu entry, implement a generic single-action state-bound native controller that checks the advertised action and the frozen fingerprint, holds the same `/tmp/rsi-sts2-native-8080.lock` as `rsi.campaign`, and stores a trace. Its first frozen action is `continue_game_over` from the fingerprint above. Observe each resulting screen before committing the next action; never retry uncertain delivery without fresh state.

Menu step 1 at SHA `1fe341396b8c02035816e5889ff8a35835e7f43b` used `python3 -m rsi.native_step --choice experiments/E045/menu-step1.json --expected-run-id EFUZ4NHFCXBT --output artifacts/runs/e045-menu-step1.json --execute`. One accepted `continue_game_over`, 4.247 seconds, no rejection, trace hash in `menu-step1-result.json`. The settled native `GAME_OVER` now advertises only `return_to_main_menu` and reports the prior E042 defeat save verified (`save_status=verified`, `is_victory=false`). This post-close verification was appended to PR #76. Commit `menu-step2.json` bound to the new fingerprint, then run the same single-action command with that choice and `e045-menu-step2.json` output; inspect the resulting menu before any start action.

Menu step 2 at SHA `64116f4f8107d3b0880216872d5386e820b32d14` accepted `return_to_main_menu`, 2.108 seconds, no rejection. The state is `MAIN_MENU`, run ID `run_unknown`, with `open_character_select` advertised. `menu-step2-result.json` holds its trace hash/fingerprint. Commit `menu-step3.json` to open character selection, then run `python3 -m rsi.native_step --choice experiments/E045/menu-step3.json --expected-run-id run_unknown --output artifacts/runs/e045-menu-step3.json --execute`. No run seed has yet been generated.

Menu step 3 at SHA `b57533673cd28913eb8f079b1593ba371bf57767` accepted `open_character_select`, 0.372 seconds, no rejection. The native `CHARACTER_SELECT` already has Ironclad selected, ascension0, seed unset, and `embark` legal. `menu-step3-result.json` records the trace hash/fingerprint. Commit `menu-step4.json` bound to that exact state, then run `python3 -m rsi.native_step --choice experiments/E045/menu-step4.json --expected-run-id run_unknown --output artifacts/runs/e045-menu-step4.json --execute`. This is the single normal generated attempt; after this action there is no reroll.

Menu step 4 at SHA `db331012686c9d42ad7dc5307efc99a88c6ba9cb` accepted `embark`, 4.98 seconds, no rejection. The next normal native run is **`4BD4R62AQHNK`**, Ironclad A0 at floor1 with 80/80 HP and 99 gold; the native run object did not export a separate seed field. The state is Neow's initial event; `menu-step4-result.json` preserves its trace hash and fingerprint. The three options are random rare card without immediate cost, one random transform, or Greed curse plus 333 gold. For immediate strength with no known shop route, commit state-bound `neow-choice.json` selecting Arcane Scroll index0; this is one Astra strategic opener, with no claim about its yet-unseen random result. Execute `python3 -m rsi.native_step --choice experiments/E045/neow-choice.json --expected-run-id 4BD4R62AQHNK --output artifacts/runs/e045-neow-choice.json --execute` and inspect the actual reward before planning floor1.

Neow choice at SHA `f0372e26ed1ebd181d9ebfcc53f6e71ee7a7e928` accepted Arcane Scroll, 0.37 seconds, no rejection. It added **Impervious** (`IMPERVIOUS`) to the starter deck at 80/80 HP; `neow-choice-result.json` has the raw trace hash and settled state fingerprint. The event now advertises only Proceed at option0. Commit `neow-proceed.json` bound to that state and execute `python3 -m rsi.native_step --choice experiments/E045/neow-proceed.json --expected-run-id 4BD4R62AQHNK --output artifacts/runs/e045-neow-proceed.json --execute`, then inspect the actual map before assigning route value. Impervious may be useful for a future burst phase if drawn, but its actual contribution is untested.

Neow Proceed at SHA `d36e4aaff8e5f9a6623edcdfe2bded5160dc9a3d` accepted one action, 0.281 seconds, no rejection, arriving at floor1 MAP with 80 HP. `neow-proceed-result.json` contains the trace hash and current map fingerprint. Initial legal nodes are left Monster row1/col2 (option0) and right Monster row1/col5 (option1). The left path offers two early Monster rewards and then an optional Rest Site at row6; the right path has early Unknown/Shop options. With full HP and Impervious, choose left to develop the deck before deciding whether to take an elite. Commit `floor1-plan.json` plus exact `floor1-route.json` bound to the map fingerprint. Execute `python3 -m rsi.campaign --expected-run-id 4BD4R62AQHNK --combat-policy floor_guided --floor-plan experiments/E045/floor1-plan.json --expert-choice experiments/E045/floor1-route.json --pause-on-danger --danger-hp 8 --max-actions 80 --max-seconds 300 --max-usd 0.10 --output artifacts/runs/e045-floor1.json --execute`. This segment stops at floor2 MAP, native terminal or explicit safety boundary; record the actual battle and reward card.

Floor1 plan at SHA `28ab0336aa3d75bfbf1aec6fd37b5a97c1cf1be9` executed the exact left route, then Jev defeated a Sludge Spinner and selected Molten Fist from Iron Wave/Twin Strike/Molten Fist. 17 accepted actions, 0 rejections, 11 Jev requests/$0.001965222, 47.036 seconds. HP fell 80→62 in battle and Burning Blood restored it to68/80 at floor2 MAP; no potion was used. `floor1-result.json` contains the trace hash/fingerprint. This is one hallway win, not a run result. The next node is a forced Monster.

Trace diagnosis: on turn3 the Spinner had 2 HP and a 14-damage intent, while the player had 1 energy, 0 block, and three legal Defends. Jev still ended the turn, taking 14; a single basic Defend would have reduced the damage by 5. Frozen pre-action fingerprint `4fc73707aae030e966b18fc28915f45d373ed8d08590a657287e2b04a36a9016`. The existing E015 `filter_end_turn` reports `excluded=true` with all three Defends as witnesses on this exact raw state, but `rsi.campaign` does not call that guard. This is a distinct native integration experiment, opened as E046 before any implementation. E045 pauses at the safe floor2 MAP while the guard is evaluated; it will continue the same run after any supported merge, without replaying the first fight.

E046/#82 completed separately and merged via `e8ccf125cf72e831bbcce63a831b22f7482359bf`; this E045 branch incorporated that merge without rewriting its earlier history. Its one bound floor2 plan resumed the same run and beat a Corpse Slug: 18 accepted actions, 0 rejections, 13 Jev calls/$0.002572416, 55.47 seconds, HP68→59 during combat and65 after Burning Blood. Jev used an Explosive Ampoule, then claimed a same-kind Ampoule reward and chose Dismantle. The guard checked 12 combat decisions and excluded zero end turns in this live room; frozen E045 turn3 and controls established the mechanism. `floor2-result.json` links the tested E046 SHA, exact trace hash and run continuation. Current floor3 MAP fingerprint is `b64cc5280084e11ea3e052eecf9365187e28ed39faed1dd809c133c4cc4eb84a`; its only next node is Unknown.

Commit `floor3-plan.json` bound to that exact MAP, then execute `python3 -m rsi.campaign --expected-run-id 4BD4R62AQHNK --combat-policy floor_guided --floor-plan experiments/E045/floor3-plan.json --pause-on-danger --danger-hp 8 --max-actions 80 --max-seconds 300 --max-usd 0.10 --output artifacts/runs/e045-floor3.json --execute`. The plan covers the unknown room, including any actual event/combat/reward Jev decisions, through floor4 MAP. Record all options, HP and reward outcomes after the native room resolves; do not infer the event from its Unknown map icon.

Floor3 at SHA `ab828578918dc52c4a6e6e67cdd31ee322e3024b` was the Self Help Book event. Jev chose Read the Back (an attack-card Sharp+2 enchantment), then selected Molten Fist; its displayed damage rose 10→12, and it still exhausts. Four accepted actions, 0 rejections, 2 Jev calls/$0.000329364, 4.317 seconds, HP remained65/80. `floor3-result.json` has the trace hash/fingerprint. Dismantle was another legal enchantment target; this unpaired choice does not establish which target maximizes the run's win chance. The only next node is another Unknown row4/col2.

Commit state-bound `floor4-plan.json`, then execute `python3 -m rsi.campaign --expected-run-id 4BD4R62AQHNK --combat-policy floor_guided --floor-plan experiments/E045/floor4-plan.json --pause-on-danger --danger-hp 8 --max-actions 80 --max-seconds 300 --max-usd 0.10 --output artifacts/runs/e045-floor4.json --execute`. Stop at floor5 MAP, terminal or explicit safety boundary, and record the actual event/combat before planning the next Monster.

Floor4 at SHA `7413b25a9130da7eb1fd0193c847b6119692dc73` resolved a Spiraling Whirlpool enchantment: Jev selected a basic Strike for Replay1, so that deck card now displays an extra play. Four accepted actions, 0 rejections, 1 Jev call/$0.00021126, 4.384 seconds, HP65→65. `floor4-result.json` contains the trace hash/fingerprint. The trace's first post-map EVENT snapshot still carried the prior finished Self Help Book; the next forced Proceed yielded the actual Whirlpool card selection. No uncertain action was retried and the final native deck confirms Replay1. This stale-looking event transition deserves separate compatibility audit if repeated, but this segment completed normally. The only next node is Monster row5/col3.

Commit bound `floor5-plan.json` and execute `python3 -m rsi.campaign --expected-run-id 4BD4R62AQHNK --combat-policy floor_guided --floor-plan experiments/E045/floor5-plan.json --pause-on-danger --danger-hp 8 --max-actions 80 --max-seconds 300 --max-usd 0.10 --output artifacts/runs/e045-floor5.json --execute`. Stop at floor6 MAP or explicit boundary, then decide the upcoming Rest Site versus Elite from actual HP/resources rather than preselecting it now.

Floor5 at tested SHA `a238d4af7c631271680d6927dd89157cd6dffc32` was a Sea Thug. Jev played Replay1 Strike then Bash on turn1, took 11 damage, then used Molten Fist and Dismantle for lethal on turn2. HP65→54→60 after Burning Blood, gold127→146, Explosive Ampoule retained. It claimed both gold and card reward, choosing Colossus over Havoc, Stomp and skip. Ten accepted actions, 0 rejections, 6 Jev calls/$0.001237698, 24.417 seconds. `floor5-result.json` records the trace. The 11-damage loss is not proven avoidable without a longer counterfactual: a turn1 Defend would delay the two-turn lethal. At floor6 MAP there are Rest Site and Elite branches. With 60 HP, Impervious, potion, and a Rest Site two nodes beyond the Elite, choose the Elite to gain a relic; this is a prospective decision, not an outcome claim.

Commit bound `floor6-plan.json` and execute `python3 -m rsi.campaign --expected-run-id 4BD4R62AQHNK --combat-policy floor_guided --floor-plan experiments/E045/floor6-plan.json --pause-on-danger --danger-hp 8 --max-actions 80 --max-seconds 300 --max-usd 0.10 --output artifacts/runs/e045-floor6.json --execute`. Stop at floor7 MAP, terminal, or safety boundary and inspect HP before the next Unknown.

The chosen Elite was a four-Phantasmal-Gardener encounter, much more dangerous than the planned single-opponent examples. At turn8, the controller had accepted 26 actions (partial raw trace SHA `9bb02a7b4b74057aea99189471c97f5ee5bac8668c240605936370f897d30cb4`) without reaching the floor boundary. I interrupted during `wait_until_actionable` after its confirmed 26th action, then established fresh actionable state: 13 HP, two enemies at 13/30 HP, 24 incoming, Explosive Ampoule available. This is a safety intervention, not a completed room result. Commit exact `floor6-rescue-1.json`, execute only the potion with `python3 -m rsi.campaign --expected-run-id 4BD4R62AQHNK --combat-policy planned --expert-choice experiments/E045/floor6-rescue-1.json --max-actions 1 --max-seconds 60 --max-usd 0.01 --output artifacts/runs/e045-floor6-rescue-1.json --execute`, then re-read state; do not assume damage or block effects before they resolve.

The potion execution at tested SHA `ace2346` was accepted, 0 rejections/0 Jev calls, and dealt exactly 10 damage to each Gardener without adding block. The fresh state is 13 HP, enemies3/20 HP, 24 incoming. A normal Strike kills the 3-HP enemy while retaining two energy for two Defends against the remaining 15 incoming, leaving 8 HP after end turn if intents resolve as displayed. Commit `floor6-rescue-2.json` and execute that exact Strike as one action using the same expert-choice command shape, with output `artifacts/runs/e045-floor6-rescue-2.json`. Recompute the hand and targets afterward.

The bound ordinary Strike at tested SHA `4eece93` killed the 3-HP Gardener; the other remains at 20 HP, 15 incoming, with 2 energy and both Defends in hand. It used 0 Jev calls and had no rejection. Commit exact `floor6-rescue-3.json`, execute one Defend with the same one-action expert command and output `artifacts/runs/e045-floor6-rescue-3.json`, then inspect the remaining hand/block.

The first Defend at tested SHA `de9679f` was accepted without rejection; 13 HP, 5 block, 1 energy and a second Defend remain against 15 incoming. Commit `floor6-rescue-4.json` and execute only that second Defend with output `artifacts/runs/e045-floor6-rescue-4.json`. Re-read before ending turn.

The second Defend at tested SHA `91aa0a4` was accepted without rejection. Native state now says 13 HP, 10 block, 0 energy against 15 incoming and `end_turn_will_kill_player=false`. Commit `floor6-rescue-5.json`, execute exactly one end turn with output `artifacts/runs/e045-floor6-rescue-5.json`, and inspect turn9. This avoids delegating the 8-HP decision to an unreviewed macro.

The bound end turn at tested SHA `0baa68f` was accepted; HP13→8, one Gardener remains at 20 HP, and its turn9 intent is Buff/Enlarge. Hand: Bash, Strike and three Defends; no incoming this turn. Commit `floor6-rescue-6.json` for Bash to apply Vulnerable, execute one action with output `artifacts/runs/e045-floor6-rescue-6.json`, then inspect the enemy's Skittish block before spending the final energy.

At tested SHA `17a7ffa`, Bash was accepted with no rejection and no Jev call. The enemy is now 12 HP/6 block/2 Vulnerable; its intent remains Buff. One energy and a basic Strike remain. Commit `floor6-rescue-7.json`, execute only that Strike with output `artifacts/runs/e045-floor6-rescue-7.json`, and inspect the actual damage before ending the turn.

The bound Strike at tested SHA `075400e` was accepted with no rejection: enemy12 HP/6 block→9 HP/0 block, still 2 Vulnerable. HP8, energy0, Buff intent. Commit `floor6-rescue-8.json` and execute one end turn with output `artifacts/runs/e045-floor6-rescue-8.json`; inspect next hand before any attack.

The bound end turn at tested SHA `065119c` was accepted without rejection. Turn10 draws Dismantle with the last Gardener at 9 HP, one Vulnerable and 11 incoming, while player HP is 8. A direct Dismantle first hit exceeds 9 damage. Commit exact `floor6-rescue-9.json` and execute one lethal action with output `artifacts/runs/e045-floor6-rescue-9.json`; verify native reward screen afterward rather than assuming victory.

The Dismantle at tested SHA `848236b` was accepted and won the native Elite battle: HP8→14 from Burning Blood, no rejection or Jev call. Reward screen has 43 gold, the relic named Throat Guard, and an unclaimed card reward; the Explosive Ampoule was spent. This proves battle survival but not yet reward collection or the act. Commit this result, then execute `python3 -m rsi.campaign --expected-run-id 4BD4R62AQHNK --combat-policy planned --max-actions 3 --max-seconds 60 --max-usd 0.02 --output artifacts/runs/e045-floor6-rewards-open.json --execute` to claim those three visible rewards and stop before the card choice. Inspect the exact offered cards and choose from the actual state.

At tested SHA `8683e58`, all three reward claim actions were accepted with no rejection or Jev call: gold146→189, relic `GORGET` (start each battle with Plating), and a card choice of Taunt, Headbutt or another Colossus. The exact card text of Headbutt is 9 damage plus returning a card from discard to the top of draw. Since this deck has low attack density and relies on Bash/Dismantle synergy, precommit `floor6-card-choice.json` to choose Headbutt; its benefit is a hypothesis, not a measured run improvement. Execute exactly one action with `python3 -m rsi.campaign --expected-run-id 4BD4R62AQHNK --combat-policy planned --expert-choice experiments/E045/floor6-card-choice.json --max-actions 1 --max-seconds 60 --max-usd 0.01 --output artifacts/runs/e045-floor6-card-choice.json --execute`, then proceed to MAP separately.
