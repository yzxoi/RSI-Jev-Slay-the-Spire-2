# E049 — Reusable state-bound native single-step controller

Issue #88. Hypothesis: promoting the generic E045 `rsi.native_step` module to main allows run/menu/rescue single actions to use the same one-writer lock, exact state binding and raw trace discipline without checking out a closed experiment branch. This is execution compatibility, not a battle or win-rate improvement.

Baseline main SHA `8567df3eddba05a918c10a1144713aec816aa2e0` does not contain `rsi.native_step`. E048's precommitted `python3 -m rsi.native_step --choice experiments/E048/menu-step1.json --expected-run-id 4BD4R62AQHNK --output artifacts/runs/e048-menu-step1.json --execute` failed at Python import (`No module named rsi.native_step`) before any MCP action; the live native state remained the E045 verified defeat summary. The source module first appeared on E045 at `1fe341396b8c02035816e5889ff8a35835e7f43b`. Only its generic controller may be copied; no run-specific E045 plan is promoted.

`verified-defeat-choice.json` duplicates E048's exact choice for a **read-only** check: run `4BD4R62AQHNK`, `GAME_OVER` summary fingerprint `2525268fd18a8c307346dd844d838def2eb389e95094c6585bd753a267966b7f`, legal `return_to_main_menu`. Precommit implementation before executing the positive check **without** `--execute`: `python3 -m rsi.native_step --choice experiments/E049/verified-defeat-choice.json --expected-run-id 4BD4R62AQHNK --output artifacts/runs/e049-read-only.json`. It must report `read_only_ready`, zero accepted game actions, and leave the same fingerprint. A wrong-hash fixture must error without action. Unit suite must pass. Record exact tested SHA, command, game/MCP versions, raw trace hash and limitations; merge only if both read-only and tests pass. E048 will execute the actual menu action after the merged tool is incorporated.
